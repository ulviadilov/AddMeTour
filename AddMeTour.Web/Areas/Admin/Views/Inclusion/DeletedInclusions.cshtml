@model List<InclusionViewModel>
<div class="main-content">
    <div class="page-content">
        <div class="container-fluid">
            <div class="row y-gap-20 justify-between items-end pb-60 lg:pb-40 md:pb-32">
                <div class="col-auto">

                    <h1 class="text-30 lh-14 fw-600">All Deleted Inclusions</h1>

                </div>
                <div class="col-auto">

                    <a asp-action="index" asp-controller="inclusion" class="button h-50 px-24 -dark-1 bg-blue-1 text-white">
                        Back
                    </a>

                </div>
            </div>
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">Inclusion</th>
                        <th scope="col">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (InclusionViewModel inclusionVM in Model)
                    {
                        <tr>
                            <td>@inclusionVM.InclusionString</td>
                            <td>
                                <a asp-area="admin" asp-action="recover" asp-controller="inclusion" asp-route-Id="@inclusionVM.Id" class="button -md -dark-1 bg-blue-1 text-white-1">Recover</a>
                                <a asp-area="admin" asp-action="harddelete" asp-controller="inclusion" asp-route-Id="@inclusionVM.Id" style="color:white; background-color:red" class="btn btn-primary">Permanently Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>